#! /bin/bash

echo "Enter any 3 nr decimal no:"
read num

dalitais=$(( num/ 2))
atlikums=$(( num% 2))
fst=$((num / 2))
snd=$fst
trd=$snd

#while [ $num -gt 0 ]; do
	
	
	echo $(( fst / 2 ))
	echo $(( snd / 2 ))
	echo $(( trd / 2 ))



	#exit 1
#done
